@model IEnumerable<ccar.Models.MeetingMinutesModel>

@{
    ViewBag.Title = "MeetingMinutesList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<head>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="~/Content/Site.css" rel="stylesheet" />

</head>
<h2>ReasonList</h2>
<p>

    @*@Html.ActionLink("Create New", "Create")*@
    <button id="addBtn" class="btn btn-outline-primary" onclick="location.href='@Url.Action("Create", "MeetingMinutes")'"><i class="fa fa-plus-square"></i> Add new note</button>

</p>

<div class="container-fluid" style="align-content:center; width:100%; margin-right: auto;margin-left: auto;">
    @foreach (var item in Model)
    {
    <div class="row" >
        <div class="col-lg-12">

            <div class="panel panel-primary">
                <div class="panel-heading">@Html.DisplayFor(modelItem => item.CategoryId)</div>
                <div class="panel-body">@Html.DisplayFor(modelItem => item.Contents)</div>
                <div class="panel-footer">

                    <div class="btn-group">
                        <button type="button" class="btn btn-success" style="color:white;" onclick="location.href='@Url.Action("Create", "MeetingMinutes")'">Edit</button>
                        <button type="button" class="btn btn-danger" style="color:white;" onclick="location.href='@Url.Action("Create", "MeetingMinutes")'">Delete</button>
                    </div>
                </div>
            </div>
      
        </div>
        <div class="col-lg-2"></div>


    </div>
    }
    </div>



