$('#filter').editableSelect({
    // enable filter
    filter: true
});
//.on('select.editable-select', function (e, li) {
//    $('#no-filter').val(li.attr('value'));
//            });

$('#filter').editableSelect({ filter: true, effects: 'slide' });
